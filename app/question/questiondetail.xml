<Page navigatingTo="onNavigatingTo" xmlns="http://schemas.nativescript.org/tns.xsd">

  <ActionBar>
    <NavigationButton tap="onBackButtonTap" android.systemIcon="menu_back" />
    <Label class="ibm7 h2" text="{{ title }}" />
  </ActionBar>

  <StackLayout class="container">
    <Label class="ibm7 h3" text="Question: " marginLeft="8"></Label>
    <TextView>
      <FormattedString>
        <Span class="pattaya" text="{{ html }}" textWrap="true" marginLeft="8" visibility="{{ type === 'html' ? 'collapse' : 'visible' }}" />
        <Span class="pattaya" text="{{ title === '' ? '' : title }}" textWrap="true" marginLeft="8" visibility="{{ type === 'html' ? 'visible' : 'collapse' }}" />
      </FormattedString>
    </TextView>
    <Label class="ibm7 h3" text="Answers: " marginLeft="8"></Label>
    <TextView>
      <FormattedString>
        <Span class="pattaya" text="{{ html }}" textWrap="true" marginLeft="8" visibility="{{ type === 'html' ? 'collapse' : 'visible' }}" />
        <Span class="pattaya" text="{{ title === '' ? '' : title }}" textWrap="true" marginLeft="8" visibility="{{ type === 'html' ? 'visible' : 'collapse' }}" />
      </FormattedString>
    </TextView>

  </StackLayout>

</Page>
